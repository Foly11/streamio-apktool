# 📱 تعليمات التثبيت السريع - سينما ستريم

## 🚀 خطوات سريعة للتجميع والتثبيت

### 1️⃣ تحضير الهاتف
```bash
# تثبيت Termux من:
# https://github.com/termux/termux-app/releases
# أو F-Droid: https://f-droid.org/packages/com.termux/
```

### 2️⃣ نقل ملفات المشروع
- انسخ مجلد `StreamingApp` إلى التخزين الداخلي لهاتفك
- افتح Termux

### 3️⃣ تشغيل الأوامر في Termux
```bash
# الانتقال إلى مجلد المشروع
cd /storage/emulated/0/StreamingApp

# أو إذا كان في مجلد التحميلات:
cd /storage/emulated/0/Download/StreamingApp

# جعل السكريبت قابل للتنفيذ
chmod +x build.sh

# تثبيت أدوات البناء (مرة واحدة فقط)
./build.sh install-tools

# بناء التطبيق
./build.sh build

# تثبيت التطبيق
./build.sh install
```

### 4️⃣ إذا فشل التثبيت التلقائي
```bash
# ستجد ملف APK في:
ls build/outputs/apk/

# انسخ الملف وثبته يدوياً من مدير الملفات
```

## ⚡ أوامر سريعة

### بناء سريع
```bash
./build.sh build
```

### تنظيف وإعادة بناء
```bash
./build.sh clean
./build.sh build
```

### تثبيت فقط
```bash
./build.sh install
```

## 🔧 حل المشاكل الشائعة

### خطأ: Permission denied
```bash
chmod +x build.sh
```

### خطأ: No such file or directory
```bash
# تأكد من أنك في المجلد الصحيح
pwd
ls -la build.sh
```

### خطأ في التثبيت
```bash
# ثبت يدوياً من مدير الملفات
# أو استخدم:
pm install build/outputs/apk/CinemaStream-debug.apk
```

### نفاد مساحة التخزين
```bash
# نظف ملفات البناء
./build.sh clean

# أو احذف مجلد البناء
rm -rf build/
```

## 📋 متطلبات النظام

- **نظام التشغيل**: Android 5.0+ (API 21)
- **ذاكرة الوصول العشوائي**: 2GB على الأقل
- **التخزين**: 500MB مساحة فارغة
- **الإنترنت**: مطلوب للتحميل والتشغيل

## 🎯 نصائح مهمة

1. **تأكد من تمكين "المصادر غير المعروفة"** في إعدادات الأمان
2. **استخدم WiFi** لتحميل الأدوات (قد تستغرق وقتاً)
3. **أغلق التطبيقات الأخرى** أثناء البناء لتوفير الذاكرة
4. **احتفظ بنسخة من APK** بعد البناء الناجح

## 📞 دعم سريع

### إذا لم يعمل شيء:
1. أعد تشغيل Termux
2. أعد تشغيل الهاتف
3. احذف مجلد build وأعد المحاولة
4. تأكد من الاتصال بالإنترنت

### للحصول على مساعدة:
- اقرأ ملف README.md للتفاصيل الكاملة
- تحقق من ملفات الأخطاء في مجلد build
- جرب البناء مرة أخرى بعد إعادة التشغيل

---

**استمتع بالمشاهدة! 🎬**